import java.util.scanner;
public class PunctuationRemoval {
    public static void main(String][]args){
        scanner scanner=new scanner(System.in);
        system .out.print("Enter any number with comma:");
        string inputNumber=scanner.next();
        int len=inputNumber.length();
        string firstsome=inputNumber.substring(0,len-4);
        String lastThree=inputNumber.substring(beingindex:len-3);
        sustem.out,println(firstsome+lastsome);
    }
}
